Sources for the plugin `plugin.appodeal.Smaato`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.appodeal.Smaato" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
